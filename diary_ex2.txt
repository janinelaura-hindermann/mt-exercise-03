- modified main.py be found under main_modified.py in the scripts folder
- creates new directory for storing the log files

Parameters used for model training: standard parameters, varying dropout

(
  cd $tools/pytorch-examples/word_language_model &&
    CUDA_VISIBLE_DEVICES=$device OMP_NUM_THREADS=$num_threads python3 main.py \
      --data $data/familyguy \
      --epochs 40 \
      --log-interval 10 \
      --emsize 200 --nhid 200 --dropout 0.0 --tied \
      --save $models/model.pt \
      --mps \
      --save-perplexities \
      --log-file $logs/perplexities_0.2.txt
)

log interval to 10 to get the almost very last perplexity value per epoch

then clean the log folder with clean_log_folder.py

then create dataframes with create_log_table.py

then create plots with plot_perplexity.py